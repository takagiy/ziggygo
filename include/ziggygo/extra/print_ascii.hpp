#ifndef COM_GITHUB_TAKAGIY_ZIGGYGO_EXTRA_PRINT_ASCII
#define COM_GITHUB_TAKAGIY_ZIGGYGO_EXTRA_PRINT_ASCII

#include <cstddef>

namespace ziggygo {
    namespace extra {
        auto print_ascii(const map &map, std::size_t dots_per_char) {}
    }
}

#endif
