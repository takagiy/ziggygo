#include <cstddef>

namespace ziggygo {
    struct point;

    struct line;

    struct cart;

    struct block;

    struct path;

    template <std::size_t, std::size_t>
    class map;

    template <std::size_t, std::size_t>
    class solver;
};
